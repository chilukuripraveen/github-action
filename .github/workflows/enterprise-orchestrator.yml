name: App Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:

  build-1:
    runs-on: ubuntu-latest

    steps:
      - name: Build Application 1
        run: |
          echo "Build 1 started"
          sleep 10
          echo "Build 1 completed"


  build-2:
    runs-on: ubuntu-latest

    steps:
      - name: Build Application 2
        run: |
          echo "Build 2 started"
          sleep 10
          echo "Build 2 completed"
